# Let’s ignore spelling mistakes for now
spelling-error-in-binary

# We do not have control of which flags are used during build
hardening-no-bindnow

# Upstream does not seem to properly use visibility=hidden and manually
# export a reduced set of symbols as explained here:
# https://wiki.debian.org/UsingSymbolsFiles#Proper_visibility_for_C.2B-.2B-_symbols
no-symbols-control-file

# Package has multiple .so libraries that are shipped as a bundle
package-name-doesnt-match-sonames libQshQmiIDL2 libQshSession2
