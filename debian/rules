#!/usr/bin/make -f

export DH_VERBOSE=1

%:
    dh $@

override_dh_auto_configure:
    :

override_dh_auto_build:
    :

override_dh_auto_clean:
    :

override_dh_auto_install:
    :

override_dh_install:
    dh_install --sourcedir=data

override_dh_strip:
    :

override_dh_dwz:
    :

override_dh_shlibdeps:
    :

override_dh_fixperms:
    :
